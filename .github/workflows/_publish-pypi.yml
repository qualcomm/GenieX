name: Publish PyPI

# Placeholder for production PyPI (stable tags only).

on:
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Placeholder
        run: 'echo "TODO: publish ${{ inputs.release_tag }} to PyPI."'
