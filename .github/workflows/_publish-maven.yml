name: Publish Maven

# Placeholder for Android AAR -> Maven Central.

on:
  workflow_call:
    inputs:
      release_tag:
        required: true
        type: string
      is_prerelease:
        required: true
        type: boolean

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Placeholder
        run: 'echo "TODO: publish ${{ inputs.release_tag }} to Maven Central (prerelease=${{ inputs.is_prerelease }})."'
