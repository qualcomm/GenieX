name: Sign Windows Artifacts

# Placeholder for Authenticode signing of the CLI installer.
# Distinct from the HTP driver catalog signed in _build-sdk.yml.

on:
  workflow_call:
    inputs:
      artifact_name:
        description: "Name of the artifact to sign (e.g. cli-installer-windows-arm64)"
        required: true
        type: string

permissions:
  contents: read

jobs:
  sign:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Placeholder
        shell: bash
        run: 'echo "TODO: sign ${{ inputs.artifact_name }}."'
